# syntax=docker/dockerfile:1
# Stream pipeline image — Quix + core; no FastAPI/UI assets required at runtime.

FROM python:3.12-slim AS base

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1

WORKDIR /app

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        libpq5 \
        curl \
        ca-certificates \
    && rm -rf /var/lib/apt/lists/*

FROM base AS builder

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        gcc \
        libpq-dev \
    && rm -rf /var/lib/apt/lists/*

COPY pyproject.toml README.md ./
COPY src ./src
COPY alembic.ini ./
COPY alembic ./alembic

RUN pip install --prefix=/install ".[pipeline]"

FROM base AS runtime

COPY --from=builder /install /usr/local
COPY config/alerts.yaml /config/alerts.yaml
COPY alembic.ini /app/alembic.ini
COPY alembic /app/alembic
COPY docker/entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh

RUN useradd --create-home --uid 10001 --shell /usr/sbin/nologin appuser \
    && chown -R appuser:appuser /app
USER appuser

ENV ALERT_CONFIG_PATH=/config/alerts.yaml \
    KAFKA_BOOTSTRAP_SERVERS=kafka:29092 \
    KAFKA_INPUT_TOPIC=logs \
    KAFKA_CONSUMER_GROUP=alert-pipeline \
    LOG_LEVEL=INFO \
    RUN_MIGRATIONS=1

ENTRYPOINT ["/entrypoint.sh"]
CMD ["alert-pipeline"]
